# Archive

Deprecated code kept for reference only — not deployed or maintained.

| Path | Notes |
|------|-------|
| `frontend-legacy/` | Original root `frontend/` (Tailwind + Vite). **Active UI:** `services/frontend/` (K8s deploy target). |

Moved in Sprint 14 (S14-DA-005).
